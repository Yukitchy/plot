{
  "name": "sample",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf docs && vite build && mv dist docs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.9.0"
  },
  "dependencies": {
    "@googlemaps/js-api-loader": "^1.13.11",
    "dayjs": "^1.11.0"
  }
}
